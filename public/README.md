Drop your logo files here with these exact names and the dashboard will pick
them up automatically (same files you're already using in the other two
dashboards):

- vita-mojo-logo.svg  (or .png)
- subway-logo.svg     (or .png)

If a file is missing, the header just falls back to text instead of breaking.
